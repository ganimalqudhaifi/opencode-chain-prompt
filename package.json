{
  "name": "opencode-chain-prompt",
  "version": "0.1.0",
  "description": "OpenCode plugin for sequential chain prompting with per-step agent selection, looping, and conditional branching",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "opencode",
    "plugin",
    "chain",
    "prompt",
    "pipeline",
    "workflow"
  ],
  "license": "MIT",
  "dependencies": {
    "gray-matter": "^4.0.3"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": "^1.17.0",
    "@opencode-ai/sdk": "^1.17.0"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.17.18",
    "@opencode-ai/sdk": "^1.17.18",
    "@types/node": "^26.1.1",
    "typescript": "^5.5.0"
  }
}
